# @tigerpingpong/shared

Shared TypeScript contracts for the TigerPingPong.ca platform.

Use this package for small, stable types that both the web app and API need.
The package currently exports the API health response shape. It should stay
domain-neutral until product, cart, checkout, auth, and admin features are
defined.
